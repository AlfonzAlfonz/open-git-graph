{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Run Extension",
			"type": "extensionHost",
			"request": "launch",
			"args": [
				"--extensionDevelopmentPath=${workspaceFolder}",
				"" // Change this to open vscode in different folder
			],
			"preLaunchTask": "${defaultBuildTask}"
		}
	]
}
